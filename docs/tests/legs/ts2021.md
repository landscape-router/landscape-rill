# ts2021 接入验证（ts2021-leg）

> headscale 过渡部署、官方客户端接入、自研客户端、subnet router 的验收场景。
> 设计规范：TS2021_LEG（[../../design/legs/ts2021.md](../../design/legs/ts2021.md)）。

## TSL-01 P0 过渡部署跑通

- 关联 REQ：REQ-006 / REQ-033
- 测试层：docker e2e
- 状态：`已覆盖`
- 证据：e2e/p0_tailscale/run_p0.sh
- 说明：headscale 0.29.3 + tailscale 1.102.2（同手机 ts2021 协议栈）经 `--login-server` + preauth key 入网、内嵌 DERP、WireGuard direct 直连 0% 丢包

## TSL-02 官方 app 入网（Android）

- 关联 REQ：REQ-033
- 测试层：真机 UI（低频 smoke）
- 状态：`待补充`
- 证据：—
- 缺口：`Use an alternate server` + 交互式登录（网页/OAuth）需真机 UI 实证（REQ-033 挂账）

## TSL-03 官方 app 入网（iOS）

- 关联 REQ：REQ-033
- 测试层：真机 UI（低频 smoke）
- 状态：`待补充`
- 证据：—
- 缺口：`Use custom coordination server` + 凭据登录需真机 UI 实证

## TSL-04 自研客户端连自建服务

- 关联 REQ：REQ-021
- 测试层：集成 + e2e
- 状态：`待补充`
- 证据：—
- 缺口：协议四层栈（HTTPS 引导 → Noise IK → HTTP/2 → tailcfg 子集）未实现（P2）

## TSL-05 subnet router 广播

- 关联 REQ：REQ-021
- 测试层：集成 + e2e
- 状态：`待补充`
- 证据：—
- 缺口：mesh `routes[]` 汇总广播进自建 tailnet 未实现（依赖 TSL-04）

## TSL-06 exit 使用

- 关联 REQ：REQ-021
- 测试层：集成 + e2e
- 状态：`待补充`
- 证据：—
- 缺口：非本网流量经 tailnet exit peer 转发未实现

## TSL-07 exit 被用作

- 关联 REQ：REQ-021
- 测试层：集成 + e2e
- 状态：`待补充`
- 证据：—
- 缺口：其他节点经本rill ext 节点出口（解包 → 引擎 → tun0 → WAN）未实现

## TSL-08 DERP 中继兜底

- 关联 REQ：REQ-021 / REQ-033
- 测试层：docker e2e
- 状态：`部分覆盖`
- 证据：e2e/p0_tailscale/run_p0.sh
- 缺口：容器同网段直连已验证（direct）；**跨 NAT 场景由 DERP 兜底未实测**

## TSL-09 官方 tailnet 备份出口

- 关联 REQ：REQ-021
- 测试层：低频 smoke
- 状态：`待补充`
- 证据：—
- 缺口：同一客户端代码切换官方 URL、exit node 可用未验证

## TSL-10 密钥轮换

- 关联 REQ：REQ-021
- 测试层：集成
- 状态：`待补充`
- 证据：—
- 缺口：machine key 持久（重启 node_id 关联稳定）、node key 轮换不中断会话未验证

## 验收断言

- [x] TSL-01：headscale + derper 部署、官方客户端 `--login-server` 入网、WG 直连互通
- [ ] TSL-02：Android 交互式登录入网（真机，待实证）
- [ ] TSL-03：iOS 凭据登录入网（真机，待实证）
- [ ] TSL-04：四层协议栈跑通、netmap 长轮询持续（P2）
- [ ] TSL-05：手机直接访问 mesh 内资源（P2）
- [ ] TSL-06：exit 使用侧 NAT 生效（P2）
- [ ] TSL-07：exit 被用作路径可达（P2）
- [ ] TSL-08：跨 NAT 场景 DERP 兜底可达（待实测）
- [ ] TSL-09：官方 tailnet 备份出口可用
- [ ] TSL-10：machine key 持久、node key 轮换不中断
