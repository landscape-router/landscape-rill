# REQ-036 auth key 格式与生成规范

> 类型：需求 ｜ 状态：📌 proposed ｜ 优先级：P0 ｜ 依赖：REQ-038 ｜ 提出：2026-08-15
> 建议默认值：与控制面端口 8443 同批落地（config.rs 常量）；格式待定

## 动机

auth key 需要统一的格式、生成与分发规范（一次性/可复用/tag/过期语义已定，见 CONTROL_PLANE §6）。

## 约束

- 生成时绑定网络（归域，CONTROL_PLANE §1.5）
- 生成过程不落日志/命令行（教训 AO-01/AO-02）

## 验收标准（草案）

- T-ADMIN：auth key 生成/吊销/过期的 CLI 或管理面操作闭环；错误格式被拒绝
