# REQ-023 core/route 实现落档

> 类型：决策 ｜ 状态：✅ merged ｜ 提出：2026-08-15 ｜ 合并：2026-08-15
> 去向：ROUTE_ENGINE §4/§9 ｜ 验收场景：RTE-01 ~ RTE-04

## 动机

路由引擎核心逻辑实现闭环（47 单测累计）。

## 决策摘要

LPM 线性扫描；lookup 排序 = len 降序 → source 优先级升序；同源多 via 全返回；fallback 链 = lookup_best + reachable 谓词；策略检查点 = 调用方 policy 谓词（v1 恒放行）；Prefix 归一化存储；过短前缀校验属公告流程。
