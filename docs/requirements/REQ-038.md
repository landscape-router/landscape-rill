# REQ-038 管理面形态（前缀公告白名单配置）

> 类型：需求 ｜ 状态：📌 proposed ｜ 优先级：P0 ｜ 依赖：— ｜ 提出：2026-08-15
> 建议默认值：配置文件 / CLI / Web API（landscape-webserver 可复用）

## 动机

coordinator 需要配置前缀公告白名单（CONTROL_PLANE §3.8）、管理 auth key（REQ-036）与策略模型（REQ-020）。

## 约束

- **无默认管理员凭据**（首次启动强制设置，fail-closed）+ 配置加载即校验
- 关键配置只存服务端（教训 AO-02）
- 授权模型 + 越权测试（教训 AO-03）

## 验收标准（草案）

- T-ADMIN：白名单增删改后 netmap 版本递增、公告生效；未授权操作被拒绝
