# REQ-031 docker e2e 落档

> 类型：决策 ｜ 状态：✅ merged ｜ 提出：2026-08-15 ｜ 合并：2026-08-15
> 去向：e2e/README ｜ 验收场景：E2E-01 / FRM-10

## 动机

mesh 腿全链路路线下闭环（内核 → tun0 → 懒握手 → 加密帧全链路 ping 通）。

## 决策摘要

二进制 main.rs（JSON 配置 + tun + coord 角色）；e2e/（Dockerfile + compose + run_e2e.sh，privileged 容器 + 双节点 ping 0% 丢包）；踩坑记录（容器 DNS 劫持、getaddrinfo、CryptoProvider、EndpoointReport 0.0.0.0 → UDP connect 探测、懒握手 in-flight 守卫、compose build 旧二进制）。
