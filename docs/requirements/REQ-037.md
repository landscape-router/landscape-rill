# REQ-037 v1 存储后端选择

> 类型：决策 ｜ 状态：📌 proposed ｜ 优先级：P1 ｜ 依赖：— ｜ 提出：2026-08-15
> 建议默认值：redb（Rust 原生、单文件）优先，sqlite 备选

## 动机

coordinator 持久状态（注册表/身份绑定/主密钥/吊销列表/netmap 版本/PathMap）需要存储后端（CONTROL_PLANE §4.1）。

## 约束

- Raft 日志兼容（P2 演进，状态模型已按幂等/软硬分离设计）
- 不引入 C 依赖与构建复杂度

## 验收标准（草案）

- 持久状态重启后可恢复；损坏状态不被猜测重建
