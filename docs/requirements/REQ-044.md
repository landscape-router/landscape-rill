# REQ-044 供应链审计与依赖最小化

> 类型：需求 ｜ 状态：📌 proposed ｜ 优先级：P2 ｜ 依赖：— ｜ 提出：2026-09-01

## 动机

自 REQ-039 拆出（日志治理已落地 LOGGING）：依赖供应链安全基线。cargo audit 已先行落地（2026-09-01，进 check.yml）；依赖最小化推迟至 release 阶段。原承载的"可复现构建与产物一致性"拆至 REQ-050（发布工程问题，验收维度不同，教训 AO-06）——一条需求 = 一个可验收的行为变更。

## 约束

- cargo audit 进 CI 门禁（已落地：check.yml，见 docs/ci/README.md）
- 依赖最小化（release 阶段）：依赖树审计记录、非必要依赖去重/裁剪

## 验收标准（草案）

- CI 含 cargo audit 门禁（已达成）
- 依赖最小化：release 前依赖树审计记录 + 非必要依赖裁剪
