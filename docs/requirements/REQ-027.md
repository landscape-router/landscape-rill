# REQ-027 legs/mesh 控制面实现落档

> 类型：决策 ｜ 状态：✅ merged ｜ 提出：2026-08-15 ｜ 合并：2026-08-15
> 去向：CONTROL_PLANE §8 ｜ 验收场景：CTL-01

## 动机

mesh 腿控制面传输层实现闭环（81 单测累计）。

## 决策摘要

传输包络定稿 = 4B 大端长度 + Envelope（MsgType 10 值，proto 落地，帧长上限 1MB fail-closed）；MeshClient/CoordinatorServer（TLS 双向流 + Envelope 循环处理）；TLS 流辅助（rustls ring provider、rcgen 自签 CA 预置）；回环集成测试全流程通过。
