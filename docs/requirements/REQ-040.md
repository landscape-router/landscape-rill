# REQ-040 WebUI 配置边界

> 类型：需求 ｜ 状态：📌 proposed ｜ 优先级：P2 ｜ 依赖：REQ-038 ｜ 提出：2026-08-15

## 动机

若采用 WebUI 管理面（REQ-038 候选），需要明确配置归属边界。

## 约束

- 关键配置只存服务端（coordinator 配置文件/DB），WebUI 不持有持久配置（教训 AO-02）

## 验收标准（草案）

- WebUI 崩溃/清缓存不影响服务端配置；服务端为唯一权威
