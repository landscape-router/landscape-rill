# REQ-024 coord/ 实现落档

> 类型：决策 ｜ 状态：✅ merged ｜ 提出：2026-08-15 ｜ 合并：2026-08-15
> 去向：CONTROL_PLANE §8 ｜ 验收场景：CTL-01 / CTL-08

## 动机

coordinator 角色实现闭环（60 单测累计）。

## 决策摘要

Ed25519Signer 落地（verify_strict 防弱密钥）；Coordinator 核心（注册→netmap 版本管理；KeyDist 派生 = HKDF 主密钥、broadcast=0xFFFFFFFF；吊销→条目移除+netmap_version+++key_version++；全网 key 轮换 = 主密钥更换 + key_version++；offline 软状态；端点上报入 netmap；注册幂等不 bump 版本）。
